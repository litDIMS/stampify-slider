/******************************************************************************
 STAMPIFY Slider Engine
 Version : 5.0.0
 Config
******************************************************************************/

const SliderConfig = {

    /* 자동재생 */
    AUTO_PLAY: true,

    /* 자동재생 시간(ms) */
    INTERVAL: 5000,

    /* 무한루프 */
    LOOP: true,

    /* 애니메이션 속도 */
    ANIMATION_SPEED: 650,

    /* 모바일 기준 */
    MOBILE_BREAKPOINT: 1024,

    /* Hover 시 일시정지 */
    PAUSE_ON_HOVER: true,

    /* Drag 사용 */
    DRAG: true,

    /* Touch 사용 */
    TOUCH: true,

    /* Keyboard */
    KEYBOARD: true,

    /* Lazy Loading */
    LAZY_LOAD: true

};

Object.freeze(SliderConfig);